<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.awtr"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".Awtr_Main"
            android:label="@string/app_name" >
            <intent-filter>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name=".New_Word"
            android:label="@string/app_name"/>
        <activity 
            android:name=".Simple_Quiz"
            android:label="@string/app_name"/>
        <activity android:name=".DeleteWords" android:label="@string/app_name">
        </activity>
        <activity android:name=".Awtr_Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ModifyWord" android:label="@string/app_name"></activity>
        <activity android:name="BetterQuiz" android:label="@string/app_name"></activity>
        <service android:name="Awtr_Service"></service>
    </application>

</manifest>